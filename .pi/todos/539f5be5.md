{
"id": "539f5be5",
"title": "Pong: add win condition and match flow",
"tags": [
"pong",
"feature"
],
"status": "open",
"created_at": "2026-05-04T18:23:49.163Z"
}

Currently scores increment forever. Add a real match.

- First to 11 wins (configurable `WIN_SCORE` constant)
- Show a "PLAYER 1 WINS" / "PLAYER 2 WINS" overlay on match end
- Pause ball + input until `Space` (or `R`) restarts the match
- Reset both scores and ball state on restart
- Optional: must win by 2

Files: `src/main.js` (state machine: `serving | playing | gameover`), overlay element in `index.html`.
